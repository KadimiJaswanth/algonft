// InputMasked — masked input for simple patterns (e.g., phone)
// TODO: implement masking and accessibility (client-only)
import React from "react";

const InputMasked: React.FC = () => <input className="border p-1" placeholder="Masked input" />;

export default InputMasked;
