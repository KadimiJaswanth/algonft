// DashboardActivityFeed — activity feed for dashboard
// TODO: populate with recent wallet or platform activity (client-side only)
import React from "react";

const DashboardActivityFeed: React.FC = () => <div className="p-2">Activity feed (placeholder)</div>;

export default DashboardActivityFeed;
