// IPFSUploaderDrag — drag-and-drop uploader stub (client-side only)
// TODO: upload to IPFS via client-side gateway or user-selected provider; do not include keys
import React from "react";

const IPFSUploaderDrag: React.FC = () => (
  <div className="p-4 border-dashed border-2 rounded">Drop files here (placeholder)</div>
);

export default IPFSUploaderDrag;
