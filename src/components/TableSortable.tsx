// TableSortable — table header with sortable columns
// TODO: emit sort change events for parent to handle (client-only)
import React from "react";

const TableSortable: React.FC = () => (
  <table className="w-full"><thead><tr><th>Column</th></tr></thead></table>
);

export default TableSortable;
