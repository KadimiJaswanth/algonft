// WalletQRButton — button to display a payment/request QR code
// TODO: generate payment QR client-side; signing must be done by wallet (AlgoSigner/MyAlgoConnect)
import React from "react";

const WalletQRButton: React.FC = () => (
  <button className="px-3 py-2 border rounded">WalletQRButton (placeholder)</button>
);

export default WalletQRButton;
