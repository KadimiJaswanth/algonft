// SpinnerInline — small inline spinner
// TODO: replace with branded spinner component
import React from "react";

const SpinnerInline: React.FC = () => <span className="inline-block w-4 h-4 border-2 border-gray-300 rounded-full animate-spin" />;

export default SpinnerInline;
