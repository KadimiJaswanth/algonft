// ToastStack — stack of transient toasts
// TODO: integrate with global toast provider (client-only)
import React from "react";

const ToastStack: React.FC = () => <div className="fixed bottom-4 right-4">Toasts</div>;

export default ToastStack;
