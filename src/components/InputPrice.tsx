// InputPrice — price input with basic formatting
// TODO: validate currency and format client-side (no backend tokens)
import React from "react";

const InputPrice: React.FC = () => <input className="border p-1" placeholder="0.00" />;

export default InputPrice;
