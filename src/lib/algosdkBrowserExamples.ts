// algosdkBrowserExamples — commented examples for building unsigned txs in-browser
// TODO: these are examples only; do not include private keys or node tokens
export const exampleBuildUnsigned = (): any => {
  // Example (commented):
  // import algosdk from 'algosdk';
  // const txn = algosdk.makePaymentTxnWithSuggestedParamsFromObject({...})
  return {} as any;
};
