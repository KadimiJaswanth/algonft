// MintPlayground — page for experimenting with mint flow (client-only sandbox)
// TODO: use unsigned tx builder and wallet signer in UI; no secrets
import React from "react";

const MintPlayground: React.FC = () => <div className="p-4">MintPlayground (sandbox)</div>;

export default MintPlayground;
