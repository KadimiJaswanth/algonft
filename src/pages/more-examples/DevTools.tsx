// DevTools — developer utilities page (client-only)
// TODO: include toggles for mocks, logging, and demo data
import React from "react";

const DevTools: React.FC = () => <div className="p-4">DevTools (dev-only)</div>;

export default DevTools;
